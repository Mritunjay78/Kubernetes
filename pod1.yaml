apiVersion: v1
kind: Pod
metadata:
  name: nginx
  labels:
    app.kubernetes.io/name: nginx
    app: testapp
spec:
  containers:
  - name: nginx
    image: nginx
    ports:
      - containerPort: 80
